🛡️ Web Security Demo: OWASP Top 10 (Vulnerable vs Secure)Mô tả: Dự án demo các lỗ hổng bảo mật web phổ biến (dựa trên OWASP Top 10) và cách khắc phục chúng bằng Secure Coding trong Python Flask.Dự án bao gồm 2 phiên bản:🛑 Vulnerable App: Ứng dụng chứa lỗ hổng (SQL Injection, IDOR, Sensitive Data Exposure).✅ Secure App: Ứng dụng đã được vá lỗi (Input Validation, Parameterized Query, Authorization).🚀 Chức năng & Lỗ hổng DemoTính năngLỗ hổng (Vulnerable)Giải pháp (Secure)OWASP CategorySearch UserSQL Injection: Cộng chuỗi trực tiếp, lộ toàn bộ DB.Parameterized Query: Sử dụng ? để binding dữ liệu.A03: InjectionUser ProfileIDOR (Broken Access Control): Xem được hồ sơ người khác (Admin) bằng cách đổi URL.Authorization Check: Kiểm tra quyền sở hữu trước khi trả về dữ liệu.A01: Broken Access ControlError HandlingSecurity Misconfiguration: Lộ Stack Trace chi tiết lỗi hệ thống.Generic Error Message: Chỉ báo lỗi chung chung, giấu chi tiết kỹ thuật.A05: Security MisconfigurationLoggingSensitive Data Exposure: Ghi password vào file log.Secure Logging: Không ghi dữ liệu nhạy cảm, log warning.A09: Security Logging Failures🛠️ Cài đặt & Chạy dự án1. Yêu cầu hệ thốngPython 3.xPip2. Cài đặt thư việnChạy lệnh sau để cài các thư viện cần thiết:Bashpip install flask python-dotenv
3. Cấu hình Database & Môi trườngĐổi tên file môi trường (nếu chưa có):Tạo file .env và thêm nội dung:Ini, TOMLDB_PATH=database.db
DB_PASSWORD=secret123
Khởi tạo dữ liệu mẫu:Chạy file init_db.py để tạo database với user admin và sinhvien1.Bashpython init_db.py
💀 Kịch bản Demo Tấn công (Vulnerable App)Chạy ứng dụng lỗi:Bashpython app_vulnerable.py
(Giả sử file code lỗi của bạn tên là app_vulnerable.py)💥 Kịch bản 1: SQL InjectionMục tiêu: Lấy toàn bộ danh sách người dùng mà không cần biết tên.URL Tấn công:http://127.0.0.1:5000/search?username=' OR '1'='1
Kết quả: Trả về JSON chứa toàn bộ user trong bảng, bao gồm cả Admin.💥 Kịch bản 2: IDOR (Xem trộm hồ sơ Admin)Mục tiêu: User hiện tại (sinhvien1) xem thông tin nhạy cảm của admin.URL Tấn công:http://127.0.0.1:5000/profile?username=admin
Kết quả: Hệ thống vẫn chào "Chào sinhvien1", nhưng trả về mật khẩu và email của Admin.🛡️ Kịch bản Demo Phòng thủ (Secure App)Chạy ứng dụng bảo mật:Bashpython secure_app.py
✅ Test 1: Chặn SQL InjectionThử tấn công: http://127.0.0.1:5000/search?username=' OR '1'='1Kết quả: Trả về lỗi 400 Bad Request hoặc User not found.Lý do: Input Validation chặn ký tự lạ và Parameterized Query vô hiệu hóa mã độc.✅ Test 2: Chặn IDORThử tấn công: http://127.0.0.1:5000/profile?username=adminKết quả: Trả về lỗi 403 Forbidden ("Bạn chỉ được xem hồ sơ của chính mình").Lý do: Hệ thống kiểm tra: if target_user != current_user: return 403.📂 Cấu trúc thư mục📦 Security-Demo-Project
 ┣ 📜 app_vulnerable.py  # Code chứa lỗ hổng (Demo)
 ┣ 📜 secure_app.py      # Code đã vá lỗi (Fix)
 ┣ 📜 init_db.py         # Script tạo dữ liệu mẫu
 ┣ 📜 database.db        # SQLite Database (Tự tạo khi chạy init_db)
 ┣ 📜 .env               # Biến môi trường (Chứa secret)
 ┗ 📜 README.md          # Hướng dẫn sử dụng
⚠️ DisclaimerDự án này chỉ phục vụ mục đích giáo dục và nghiên cứu học tập trong khuôn khổ môn học An toàn thông tin. Không sử dụng các kỹ thuật này để tấn công các hệ thống thực tế mà không có sự cho phép.
